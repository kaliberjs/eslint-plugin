const test = require('node:test');

test('import/export', { todo: 'This rule is problematic to test. It requires a file system to resolve exports, but attempts to use mock-fs failed to work with the eslint-plugin-import resolver.' });
