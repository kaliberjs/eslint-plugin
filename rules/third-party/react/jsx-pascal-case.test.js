const test = require('node:test');

test('react/jsx-pascal-case', { todo: 'This rule is problematic to test. It is not being triggered by the RuleTester, even with invalid code that should be caught.' });
